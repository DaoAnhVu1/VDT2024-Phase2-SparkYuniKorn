apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: spark
spec:
  hard:
    requests.cpu: "3"      
    requests.memory: "4Gi" 
    limits.cpu: "10"         
    limits.memory: "10Gi"  
